hello

Utilisation du makefile:

	bash makefile.sh : compile et execute , le cas non echeant.
	
	bash makefile.sh doc : genere la javadoc dans le dossier doc (a creer manuellement)
	
	bash makefile.sh clean : supprime les .class